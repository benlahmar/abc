{
  "name": "fsbm-portail",
  "private": true,
  "description": "Portail web de la Faculté des Sciences Ben M'Sik — front-end React, API Node.js.",
  "type": "module",
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "dev": "concurrently -k -n api,web -c yellow,cyan \"npm run dev -w @fsbm/api\" \"npm run dev -w @fsbm/web\"",
    "build": "npm run build -w @fsbm/web && npm run build -w @fsbm/api",
    "start": "npm run start -w @fsbm/api",
    "test": "npm run test --workspaces --if-present",
    "typecheck": "npm run typecheck --workspaces --if-present"
  },
  "engines": {
    "node": ">=20.19"
  },
  "devDependencies": {
    "concurrently": "^9.2.0"
  }
}
